[project]
name = "expense-tracker"
version = "1.0.0"
description = "Track expenses"
requires-python = ">=3.10"

[project.optional-dependencies]
menu = ["textual"]

[project.scripts]
expenses = "expenses.menu:main"

[tool.setuptools.packages.find]
where=["src"]
include = ["expenses", "expense.*"]
